'using strict';

const types = {
  variable: 'variable',
  block: 'block',
  condition: 'condition'
};

/*
function parse(type) {
  if (type === 'variable') {
    return types.var;
  } else if (type === 'block') {
    return types.block;
  } else if (type === 'condition') {
    return types.condition;
  }
}
*/

module.exports = types;
